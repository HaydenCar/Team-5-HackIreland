<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= book_name %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <!-- Navigation Bar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">NoteBook</a>
      <div class="navbar-nav">
        <a class="nav-link" href="/books"><i class="fas fa-book"></i> My Books</a>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container mt-5">
    <h1 class="card-title text-center mb-4"><%= book_name %></h1>
    <div class="book-grid">
      <% notes.each do |note| %>
        <div class="book-card">
          <img src="/uploads/<%= note['filename'] %>" alt="Note Image">
          <div class="book-card-body">
            <h3 class="book-card-title"><%= note['note_name'] %></h3>
            <div class="markdown-content">
              <%= note['text'] %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <div class="d-grid mt-4">
      <a href="/books" class="btn btn-secondary btn-lg">Back to Books</a>
      <a href="/" class="btn btn-primary btn-lg mt-3">Upload Another Image</a>
    </div>
  </div>
</body>
</html>
