<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Books</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <!-- Navigation Bar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">NoteBook</a>
      <div class="navbar-nav">
        <a class="nav-link" href="/books"><i class="fas fa-book"></i> My Books</a>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container mt-5">
    <h1 class="card-title text-center mb-4">My Books</h1>
    <% if books.any? %>
      <div class="book-grid">
        <% books.each do |book_name, book_notes| %>
          <a href="/book/<%= book_name %>" class="text-decoration-none">
            <div class="book-card">
              <img src="/uploads/<%= book_notes.keys.first %>" alt="Book Cover">
              <div class="book-card-body">
                <h3 class="book-card-title"><%= book_name %></h3>
                <p class="book-card-text"><%= book_notes.size %> notes</p>
              </div>
            </div>
          </a>
        <% end %>
      </div>
    <% else %>
      <p class="text-center">No books found. Create one by saving a note!</p>
    <% end %>
  </div>
</body>
</html>